// ngitify-web/src/pages/dentist/ActivityLogs.js
// Re-uses the admin ActivityLogs component — the API already returns
// only the authenticated user's own activity when role === 'dentist'.
import ActivityLogs from '../admin/ActivityLogs';
export default ActivityLogs;
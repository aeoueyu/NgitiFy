// ngitify-web/src/pages/dentist/Notifications.js
// Re-uses the admin Notifications component — the API (/api/notifications)
// already scopes results to the requesting user via JWT.
import Notifications from '../admin/Notifications';
export default Notifications;